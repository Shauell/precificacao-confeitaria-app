erval(()=>{game.time--;sendState();render();if(game.time<=0)reveal()},1000)}}
  function answer(who,index,question){if(role!=='host'||game.phase!=='question'||question!==game.index||![0,1,2,3].includes(index)||game.answers[who]!==undefined)return;if(game.config.mode==='turns'&&game.players[game.turn].id!==who)return;game.answers[who]=index;if(index===game.current.a){const p=game.players.find(x=>x.id===who);p.correct++;p.score+=100+(game.config.seconds?Math.max(0,game.time)*2:0)}sendState();render();if(game.config.mode==='turns'||game.answers.host!==undefined&&game.answers.guest!==undefined)reveal()}
  function reveal(){if(role!=='host'||game.phase!=='question')return;stopTimers();game.phase='reveal';game.reveal={answer:game.current.a,explanation:game.current.e,reference:game.current.r,fun:game.current.f};sendState();render();advance=setTimeout(nextQuestion,game.config.delay)}
  function finish(){stopTimers();game.phase='finished';const [a,b]=game.players;game.winner=a.score===b.score?'tie':a.score>b.score?'host':'guest';sendState();processResult();render()}
  function rematch(){game.id=crypto.randomUUID?.()||String(Date.now());processed='';game.deck=prepareDeck();game.count=game.deck.length;game.index=-1;game.turn=Math.random()<.5?0:1;game.players.forEach(p=>{p.score=0;p.correct=0});nextQuestion()}
  function processResult(){if(game?.phase!=='finished'||processed===game.id)return;processed=game.id;const me=game.players.find(p=>p.id===role);profile.games++;profile.correct+=me?.correct||0;profile.xp+=(me?.correct||0)*20+20;if(game.winner===role){profile.wins++;profile.xp+=100}saveProfile()}
  function pause(){if(role!=='host')return;if(game.phase==='paused'){game.phase='question';startTickerAgain()}else if(game.phase==='question'){game.phase='paused';stopTimers()}sendState();render()}
  function startTickerAgain(){if(!game.config.seconds)return;ticker=setInterval(()=>{game.time--;sendState();render();if(game.time<=0)reveal()},1000)}
  function end(){if(role==='host'&&confirm('Encerrar a partida agora?'))finish()}

  function stateFor(viewer){const s=JSON.parse(JSON.stringify(game));s.myAnswer=s.answers?.[viewer];s.answered={host:s.answers?.host!==undefined,guest:s.answers?.guest!==undefined};delete s.answers;delete s.deck;if(s.current&&s.phase!=='reveal'&&s.phase!=='finished')delete s.current.a;return s}
  function sendState(){if(role==='host'&&conn?.open)conn.send({type:'state',state:stateFor('guest')})}
  function render(){if(!game)return;if(game.phase==='lobby')return lobby();if(game.phase==='finished')return result();play()}
  function meId(){return role==='host'?'host':'guest'}
  function other(){return game.players.find(p=>p.id!==meId())}
  function players(active){return `<div class="player-grid">${game.players.map(p=>`<div class="player ${p.id===active?'active':''} ${p.id===meId()?'me':''}"><div class="player-head"><span class="player-emoji">${p.avatar}</span><b>${esc(p.name)}</b></div><div class="score">${p.score}</div><div class="corrects">${p.correct} acertos${p.connected?'':' • desconectado'}</div></div>`).join('')}</div>`}
  function lobby(){const link=`${location.origin}${location.pathname}?room=${room}`;app.innerHTML=`<main class="shell">${head(role==='host'?'Compartilhe o código e continue neste celular':'Você entrou na sala')}<section class="card hero-code"><p>Código da sala</p><div class="code">${room}</div>${role==='host'?`<div class="copybox"><input id="link" readonly value="${esc(link)}"><button class="btn small secondary" id="copy">Copiar</button></div><div class="actions inline"><button class="btn secondary" id="share">Compartilhar</button><button class="btn ghost" id="install">Instalar</button></div>`:''}</section><section class="card">${players(null)}<div class="notice ${game.players[1].connected?'success':'warn'}">${game.players[1].connected?'Os dois jogadores estão conectados.':'Aguardando o segundo jogador entrar…'}</div>${role==='host'?`<div class="actions"><button class="btn primary" id="start" ${game.players[1].connected?'':'disabled'}>Começar partida</button><button class="btn danger" id="leave">Fechar sala</button></div>`:`<div class="waiting"><div class="spinner"></div><p>Aguardando o criador começar.</p><button class="btn ghost" id="leave">Sair</button></div>`}</section></main>`;document.getElementById('start')?.addEventListener('click',start);document.getElementById('copy')?.addEventListener('click',()=>copy(link));document.getElementById('share')?.addEventListener('click',()=>share(link));document.getElementById('install')?.addEventListener('click',installHelp);document.getElementById('leave').onclick=home}
  function play(){const s=role==='host'?stateFor('host'):game;const active=s.config.mode==='turns'?s.players[s.turn].id:null;const mine=s.myAnswer;const can=s.phase==='question'&&(s.config.mode==='simultaneous'?!s.answered[meId()]:active===meId()&&!s.answered[meId()]);const q=s.current;const turnText=s.phase==='paused'?'Partida pausada.':s.phase==='reveal'?'Resposta revelada.':s.config.mode==='turns'?(active===meId()?'Sua vez de 