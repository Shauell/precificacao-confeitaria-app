responder!':`Vez de ${esc(s.players[s.turn].name)}.`):(s.answered[meId()]?'Resposta enviada. Aguardando o outro jogador.':'Os dois podem responder.');app.innerHTML=`<main class="shell"><div class="topbar"><div><div class="room-code">${room}</div><div class="connection ${other()?.connected?'ok':'bad'}">${other()?.connected?'● conectado':'● desconectado'}</div></div>${role==='host'?`<div><button class="btn small secondary" id="pause">${s.phase==='paused'?'Continuar':'Pausar'}</button> <button class="btn small danger" id="end">Encerrar</button></div>`:''}</div><section class="card">${players(active)}<div class="progress-wrap"><div class="progress" style="width:${((s.index+1)/s.count)*100}%"></div></div><div class="question-head"><span>${esc(q.c)} • ${q.d==='dificil'?'Difícil':q.d==='medio'?'Média':'Fácil'} • ${s.index+1}/${s.count}</span>${s.config.seconds?`<span class="timer ${s.time<=5?'low':''}">${s.time}</span>`:''}</div><div class="question">${esc(q.q)}</div><div class="notice ${can?'success':''}">${turnText}</div><div class="options" style="margin-top:13px">${q.o.map((o,i)=>{const cls=['option'];if(mine===i)cls.push('selected');if(s.phase==='reveal'&&s.reveal.answer===i)cls.push('correct');if(s.phase==='reveal'&&mine===i&&mine!==s.reveal.answer)cls.push('wrong');return `<button class="${cls.join(' ')}" data-a="${i}" ${can?'':'disabled'}><span class="letter">${letters[i]}</span><span>${esc(o)}</span></button>`}).join('')}</div>${s.phase==='reveal'?`<div class="reveal notice ${mine===s.reveal.answer?'success':mine===undefined?'warn':'error'}"><h3>${mine===s.reveal.answer?'Resposta correta! 🎉':mine===undefined?'O tempo terminou ⏳':'Resposta incorreta'}</h3><p>${esc(s.reveal.explanation)}</p><p class="reference">${esc(s.reveal.reference)}</p>${s.reveal.fun?`<p>${esc(s.reveal.fun)}</p>`:''}<p>Próxima pergunta automaticamente…</p></div>`:''}</section><p class="footer-note">Mantenham a Arena aberta durante a partida.</p></main>`;document.querySelectorAll('[data-a]').forEach(b=>b.onclick=()=>{const i=+b.dataset.a;if(role==='host')answer('host',i,s.index);else if(conn?.open)conn.send({type:'answer',index:i,question:s.index})});document.getElementById('pause')?.addEventListener('click',pause);document.getElementById('end')?.addEventListener('click',end)}
  function result(){const s=role==='host'?stateFor('host'):game;const me=s.players.find(p=>p.id===meId());const title=s.winner==='tie'?'Empate!':s.winner===meId()?'Você venceu!':`${s.players.find(p=>p.id===s.winner)?.name} venceu!`;app.innerHTML=`<main class="shell">${head('Resultado final')}<section class="card result"><div class="trophy">${s.winner==='tie'?'🤝':s.winner===meId()?'🏆':'👏'}</div><h2>${esc(title)}</h2><p>${esc(me.name)}, você fez ${me.score} pontos e acertou ${me.correct} perguntas.</p>${players(null)}<div class="statline" style="justify-content:center"><span class="pill">✨ ${profile.xp} XP</span><span class="pill">🏆 ${profile.wins} vitórias</span></div><div class="actions">${role==='host'?'<button class="btn primary" id="rematch">Jogar revanche</button>':'<div class="notice">Aguardando o criador iniciar a revanche.</div>'}<button class="btn secondary" id="home">Voltar ao início</button></div></section></main>`;document.getElementById('rematch')?.addEventListener('click',rematch);document.getElementById('home').onclick=home}

  async function copy(text){try{await navigator.clipboard.writeText(text);say('Link copiado!')}catch(_){document.getElementById('link')?.select();document.execCommand('copy');say('Link copiado!')}}
  async function share(link){if(navigator.share){try{await navigator.share({title:'Arena Bíblica',text:`Entre na sala ${room} para jogar comigo!`,url:link})}catch(_){}}else copy(link)}
  function installHelp(){if(installPrompt){installPrompt.prompt();installPrompt=null;return}const ios=/iphone|ipad|ipod/i.test(navigator.userAgent);document.body.insertAdjacentHTML('beforeend',`<div class="modal-backdrop" id="modal"><div class="modal"><h2>Colocar na tela inicial 📲</h2>${ios?'<ol><li>Abra no Safari.</li><li>Toque em Compartilhar.</li><li>Escolha “Adicionar à Tela de Início”.</li></ol>':'<ol><li>Abra o menu do Chrome.</li><li>Toque em “Adicionar à tela inicial” ou “Instalar aplicativo”.</li></ol>'}<button class="btn primary" id="close">Entendi</button></div></div>`);document.getElementById('close').onclick=()=>document.getElementById('modal').remove()}
  function fatal(m){app.innerHTML=`<main class="shell">${head('Não foi possível iniciar')}<section class="card"><div class="notice error">${esc(m)}</div><div class="actions"><button class="btn primary" id="back">Voltar</button></div></section></main>`;document.getElementById('back').onclick=home}
  window.addEventListener('beforeinstallprompt',e=>{e.preventDefault();installPrompt=e});
  window.addEventListener('offline',()=>say('Sem internet. A partida pode ser interrompida.'));
  if('serviceWorker'in navigator&&location.protocol==='https:')window.addEventListener('load',()=>navigator.serviceWorker.register('./sw.js').catch(()=>{}));
  home();
})();
