ay"><option value="3500">3,5 segundos</option><option value="5000" selected>5 segundos</option><option value="7000">7 segundos</option></select></label></div><div class="notice warn" style="margin-top:14px"><b>Por turnos:</b> um responde por vez. <b>Os dois respondem:</b> o resultado aparece depois das duas respostas.</div><div class="actions"><button class="btn primary" id="open">Criar sala online</button><button class="btn ghost" id="back">Voltar</button></div></section></main>`;
    document.getElementById('back').onclick=home;
    document.getElementById('open').onclick=()=>createRoom({category:document.getElementById('cat').value,difficulty:document.getElementById('dif').value,count:+document.getElementById('count').value,seconds:+document.getElementById('seconds').value,mode:document.getElementById('mode').value,delay:+document.getElementById('delay').value});
  }

  function joinScreen(preset=''){app.innerHTML=`<main class="shell">${head('Entre pelo código mostrado no outro celular')}<section class="card"><label>Código da sala<input id="room" maxlength="6" inputmode="text" autocapitalize="characters" placeholder="ABC123" value="${esc(preset)}"></label><div class="actions"><button class="btn primary" id="enter">Entrar e jogar</button><button class="btn ghost" id="back">Voltar</button></div></section></main>`;document.getElementById('back').onclick=home;document.getElementById('enter').onclick=()=>{const c=document.getElementById('room').value.toUpperCase().replace(/[^A-Z0-9]/g,'');if(c.length!==6)return say('Digite o código de 6 caracteres.');joinRoom(c)}}

  function createRoom(cfg){if(typeof Peer!=='function')return fatal('A conexão online não carregou. Verifique a internet e recarregue.');closeNet();role='host';room=code();game={phase:'lobby',id:crypto.randomUUID?.()||String(Date.now()),config:cfg,index:-1,count:0,time:0,turn:0,current:null,answers:{},reveal:null,winner:null,players:[player('host',true),{id:'guest',name:'Aguardando…',avatar:'🕊️',score:0,correct:0,connected:false}],deck:[]};render();peer=new Peer(PREFIX+room.toLowerCase(),opts());peer.on('connection',acceptGuest);peer.on('error',e=>fatal(e.type==='unavailable-id'?'O código coincidiu com outra sala. Volte e crie novamente.':'Não foi possível abrir a sala. Tente novamente.'))}
  function player(id,connected){return{id,name:profile.name,avatar:profile.avatar,score:0,correct:0,connected}}
  function acceptGuest(c){if(conn?.open){c.close();return}conn=c;c.on('data',hostData);c.on('close',()=>{if(game){game.players[1].connected=false;sendState();render()}})}
  function hostData(m){if(!m||typeof m!=='object')return;if(m.type==='hello'){game.players[1]=Object.assign(player('guest',true),{name:String(m.name||'Jogador').slice(0,24),avatar:m.avatar||'🕊️'});sendState();render()}else if(m.type==='answer')answer('guest',+m.index,+m.question)}

  function joinRoom(c){if(typeof Peer!=='function')return fatal('A conexão online não carregou. Verifique a internet e recarregue.');closeNet();role='guest';room=c;waiting('Conectando à sala…');peer=new Peer(undefined,opts());peer.on('open',()=>{conn=peer.connect(PREFIX+room.toLowerCase(),{reliable:true});conn.on('open',()=>{conn.send({type:'hello',name:profile.name,avatar:profile.avatar});waiting('Conectado! Aguardando o criador…')});conn.on('data',m=>{if(m?.type==='state'){game=m.state;processResult();render()}});conn.on('close',()=>waiting('A conexão caiu. Volte e entre novamente com o mesmo código.'));setTimeout(()=>{if(!conn?.open)waiting('Sala não encontrada ou conexão indisponível. Confira o código e tente novamente.')},10000)});peer.on('error',()=>waiting('Não foi possível entrar. Confira o código e tente novamente.'))}
  function waiting(text){app.innerHTML=`<main class="shell">${head('Conectando os dois celulares')}<section class="card waiting"><div class="spinner"></div><p>${esc(text)}</p><button class="btn ghost" id="back">Voltar</button></section></main>`;document.getElementById('back').onclick=home}

  function prepareDeck(){let pool=Q.filter(q=>(game.config.category==='all'||q.c===game.config.category)&&(game.config.difficulty==='all'||q.d===game.config.difficulty));if(pool.length<game.config.count)pool=Q.filter(q=>game.config.difficulty==='all'||q.d===game.config.difficulty);return shuffle(pool).slice(0,Math.min(game.config.count,pool.length)).map(q=>{const order=shuffle(q.o.map((text,i)=>({text,i})));return{c:q.c,d:q.d,q:q.q,o:order.map(x=>x.text),a:order.findIndex(x=>x.i===q.a),e:q.e,r:q.r,f:q.f||''}})}
  function start(){if(!game.players[1].connected)return;game.deck=prepareDeck();game.count=game.deck.length;game.players.forEach(p=>{p.score=0;p.correct=0});game.index=-1;game.turn=Math.random()<.5?0:1;nextQuestion()}
  function nextQuestion(){stopTimers();game.index++;if(game.index>=game.count)return finish();game.phase='question';game.current=game.deck[game.index];game.answers={};game.reveal=null;game.time=game.config.seconds;if(game.config.mode==='turns'&&game.index>0)game.turn=1-game.turn;sendState();render();if(game.config.seconds){ticker=setInt